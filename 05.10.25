#include <iostream>
using namespace std;

#ifdef task_1
int main() {
    int N;
    cout << "Введи любое положительное число" << endl;
    cin >> N;
    
    int summ = 0;
    
    for (int i = 1; i <= N; i++) {
        summ = summ + i;
    }
    
    cout << "Сумма чисел от 1 и до " << N << " = " << summ << endl;
    
    return 0;
}

#endif

#ifdef task_2
int main() {
    const double ocean = 1.5;
    
    cout << "Год   Уровень океана (в мм)" << endl;
    double height = 0.0;
    
    for (int i = 1; i <= 25; i++) {
        height += ocean;
        cout << i << "     " << height << endl;
    }
    
    return 0;
}

#endif

#ifdef task_3
int main() {
    
    const double caloriesperminute = 3.6;
    
    for (int min = 5; min <= 30; min += 5) {
        cout << "За " << min << " минут вы сожгли " << min * caloriesperminute << " калорий " << endl;
    }
    
    return 0;
    
}
    
#endif

#ifdef task_4
int main() {
    double speed;
    int time;
    
    cout << "Напиши скорость транспортного средства (в милях в час)" << endl;
    cin >> speed;
    
    cout << "Напиши количество часов, сколько ехало транспортное средство" << endl;
    cin >> time;
    
    if (speed < 0 || time < 1) {
        cout << "Ошибка" << endl;
    } else {
        cout << "Время            пройденное расстояние (в милях)" << endl;
        
        for (int i = 1; i <= time; i++) {
            double distance = speed * i;
            cout << i << "                                    "<< distance << endl;
        }
        
        return 0;
    }
    
}
    
#endif

#ifdef task_5
int main() {
    int days;
    
    cout << "Введи количество дней, которое ты работал" << endl;
    cin >> days;
    
    if (days < 1) {
        cout << "Произошла ошибка. Дней не может быть меньше одного" << endl;
    } else {
        double penny = 1;
        double total = 0;
        
        cout << "День          ваш заработок (в долларах)" << endl;
        
        for (int i = 1; i <= days; i++) {
            double dollars = penny / 100;
            
            cout << i << "              " << dollars << endl;
            
            total = total + penny;
            penny = penny * 2;
        }
        
        cout << "Общая сумма вашего заработка:  " << total / 100 << " долларов" << endl;
    }
    
    return 0;
    
}

#endif

#ifdef task_6
int main() {
    int floors;
    
    cout << "Скажите, а сколько этажей в отеле?" << endl;
    cin >> floors;
    
    int totalRooms = 0;
    int occupiedrooms = 0;
    int freerooms = 0;
    
    if (floors < 1) {
        cout << "Извините, мы не принимаем ответ с числом этажей меньше 1" << endl;
    } while (floors <= 0) {
        cout << "Введи ответ с числом этажей снова" << endl;
        cin >> floors;
    } 
    
    for (int i = 1; i <= floors; i++) {
        int floors = i;
        while (floors == 13) {
            cout << "13 этаж пропускается из-за суеверия. Его не существует в нашем отеле" << endl;
            cout << "Введи ответ с числом другого этажа" << endl;
            cin >> floors;
            continue;
        } 
    }
    
    int rooms;
    
    cout << "Какое количество комнат на " << "" << "этаже?" << endl;
    cin >> rooms;
    
    while (rooms < 10) {
        cout << "На одном этаже отеля у нас находится не меньше 10 комнат" << endl;
        cout << "Введи ответ с новым числом комнат на этаже" << endl;
        cin >> rooms;
        }
    
    cout << "Скажите, а сколько комнат занято на этом этаже?" << endl;
    
    int occupied;
    cin >> occupied;
    
    while (occupiedrooms > rooms || occupiedrooms < 0) {
        cout << "Занятых посетителями комнат не может быть больше, чем всех" << endl;
        cout << "Введи число занятых посетителями комнат снова" << endl;
        cin >> occupiedrooms;
    }
    
    totalRooms += rooms;
    occupiedrooms += occupied;
    
    freerooms = totalRooms - occupiedrooms;
    double percent = (double)occupiedrooms / totalRooms * 100;
    
    cout << "Общее число всех комнат: " << totalRooms << endl;
    
    cout << "Занятые комнаты: " << occupiedrooms << endl;
    
    cout << "Свободные комнаты: " << freerooms << endl;
    
    cout << "Процент занятых (всего): " << percent << " " << endl;
    
    return 0;
    
} 

#endif

#ifdef task_8
int main() {
    double summ;
    cout << "Введи сумму, заложенную в бюджет на месяц" << endl;
    cin >> summ;
    
    while (summ < 0) {
        cout << "Ошибка. Сумма, заложенная в бюджет, не может быть отрицательной. Введи сумму снова" << endl;
        cin >> summ;
    }
    
    double expense;
    double result = 0;
    
    cout << "Для завершения операции нажми 0" << endl;
    
    while (1) {
        cout << "Введи свой расход" << endl;
        cin >> expense;
        
        if (expense == 0)
        break;
        
        while (expense < 0) {
            cout << "Ошибка. Расход не может быть отрицательным. Введи расход снова" << endl;
            cin >> expense;
        }
        
        result += expense;
        
    }
    
    double difference = summ - result;
    
    cout << "       Результат       " << endl;
    cout << "Ваш бюджет: " << summ << endl;
    cout << "Ваши расходы: " << result << endl;
    
    if (difference >= 0) {
        cout << "Вы вышли в плюс: " << difference << endl;
    } else {
        cout << "Вы вышли в минус: " << - difference << endl;
    }
    
    return 0;
    
}

#endif

#ifdef task_8
int main() {
    int number = 50;
    int N;
    
    cout << "Я решила загадать число от 10 до 80. Хочу, чтобы ты попробовал отгадать" << endl;
    
    while (1) {
        cout << "Введи любое число, которое, по-твоему мнению, я загадала" << endl;
        cin >> N;
        
    if (N > number) {
        cout << "Too high, try again" << endl;
        
    } else if (N < number) {
        cout << "Too low, try again" << endl;
        
    } else {
        cout << "Ты смог угадать мое загаданное число. Поздравляю" << endl;
        break;
        
    }
    
}
    
    return 0;
    
}

#endif
