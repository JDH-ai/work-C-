#include <iostream>
using namespace std;

//ifdef task_1
int main() {
    int N;
    cout << "Введи любое положительное число" << endl;
    cin >> N;
    
    int summ = 0;
    
    for (int i = 1; i <= N; i++) {
        summ = summ + i;
    }
    
    cout << "Сумма чисел от 1 и до " << N << " = " << summ << endl;
    
    return 0;
}

//endif

//ifdef task_2
int main() {
    const double ocean = 1.5;
    
    cout << "Год   Уровень океана (в мм)" << endl;
    double height = 0.0;
    
    for (int i = 1; i <= 25; i++) {
        height += ocean;
        cout << i << "     " << height << endl;
    }
    
    return 0;
}

//endif

